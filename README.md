# TAMARINI

"Tutor in your pocket" — a math-only step-by-step homework helper.

## Tech stack

- Next.js (React, App Router)
- Vercel for hosting
- Google Gemini (via `@google/generative-ai`) on the server
- Environment variables for API key (`GEMINI_API_KEY`)

## Local setup

1. Install dependencies:

   ```bash
   npm install
